# 🔌 Circuit Breaker Protocol

## المشكلة

عندما يفشل أمر أو أداة، يميل الوكيل إلى إعادة المحاولة بنفس الطريقة مراراً وتكراراً، مما يؤدي إلى حلقة لانهائية من الفشل وإهدار الموارد.

## الحل

بروتوكول **Circuit Breaker** (قاطع الدائرة) يمنع هذا السلوك من خلال فرض قواعد صارمة:

```
إذا فشلت نفس الأداة أو الأمر مرتين متتاليتين:
    ⚡ توقف فوراً
    ⚡ لا تعيد المحاولة بنفس الطريقة
    ⚡ ابحث عن حل بديل
```

## خطوات التنفيذ

### الخطوة 1: اكتشاف الفشل المتكرر

```xml
<circuit_breaker_check>
    <question>هل فشلت هذه الأداة/الأمر من قبل في هذه الجلسة؟</question>
    <if_yes>فعّل بروتوكول Circuit Breaker</if_yes>
    <if_no>تابع التنفيذ العادي</if_no>
</circuit_breaker_check>
```

### الخطوة 2: قراءة سجل الأخطاء

```bash
# قراءة آخر 50 سطر من سجل الأخطاء
tail -50 error.log

# أو استخدام أداة file
file read /path/to/error.log
```

### الخطوة 3: البحث عن الحل

```xml
<search_solution>
    <action>ابحث عن رسالة الخطأ المحددة على الإنترنت</action>
    <tool>search</tool>
    <query>[رسالة الخطأ] + solution</query>
</search_solution>
```

### الخطوة 4: صياغة فرضية جديدة

```xml
<new_hypothesis>
    <question>ما السبب الجذري للخطأ؟</question>
    <question>ما البدائل المتاحة؟</question>
    <question>هل هناك طريقة مختلفة تماماً لتحقيق نفس الهدف؟</question>
</new_hypothesis>
```

### الخطوة 5: تجربة نهج بديل

```xml
<alternative_approach>
    <original>pip install package</original>
    <alternatives>
        <alt1>sudo uv pip install --system package</alt1>
        <alt2>pip install --user package</alt2>
        <alt3>conda install package</alt3>
        <alt4>البحث عن بديل للحزمة</alt4>
    </alternatives>
</alternative_approach>
```

## أمثلة عملية

### مثال 1: فشل تثبيت حزمة Python

```
❌ الفشل الأول: pip install numpy
❌ الفشل الثاني: pip install numpy

⚡ تفعيل Circuit Breaker:
1. قراءة رسالة الخطأ: "Permission denied"
2. البحث: "pip install permission denied solution"
3. الفرضية: مشكلة صلاحيات
4. البديل: sudo uv pip install --system numpy ✅
```

### مثال 2: فشل طلب API

```
❌ الفشل الأول: fetch('https://api.example.com')
❌ الفشل الثاني: fetch('https://api.example.com')

⚡ تفعيل Circuit Breaker:
1. قراءة رسالة الخطأ: "429 Too Many Requests"
2. البحث: "API rate limit handling"
3. الفرضية: تجاوز حد الطلبات
4. البديل: إضافة تأخير + retry logic ✅
```

### مثال 3: فشل قراءة ملف

```
❌ الفشل الأول: file read /path/to/file
❌ الفشل الثاني: file read /path/to/file

⚡ تفعيل Circuit Breaker:
1. قراءة رسالة الخطأ: "File not found"
2. البحث: find . -name "filename"
3. الفرضية: المسار خاطئ
4. البديل: استخدام المسار الصحيح ✅
```

## قواعد ذهبية

| القاعدة | الوصف |
|---------|-------|
| **لا تكرر الفشل** | إذا فشل شيء مرتين، لا تجربه مرة ثالثة بنفس الطريقة |
| **اقرأ الخطأ** | رسالة الخطأ تحتوي على الحل غالباً |
| **ابحث أولاً** | قبل التجربة العشوائية، ابحث عن الحل |
| **فكر بشكل مختلف** | الحل قد يكون في نهج مختلف تماماً |
| **وثّق الحل** | عندما تجد الحل، وثّقه للمستقبل |

## التكامل مع سير العمل

```
المرحلة 4: EXECUTE → المرحلة 5: CIRCUIT_CHECK → المرحلة 6: HEAL
                              ↓
                    هل فشل شيء مرتين؟
                              ↓
                    نعم → فعّل البروتوكول
                              ↓
                    اقرأ → ابحث → فكر → جرب بديل
```

---

**تذكر:** الفشل ليس نهاية الطريق، بل بداية البحث عن طريق أفضل.
